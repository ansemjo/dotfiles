# safety!
# makes these commands interactive,
# i.e. prevents accidental overwrites
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'
alias rmrf='rm -rf' # read mail really fast :)

# also apply to other commands that might overwrite
#alias rename='rename -o'
