#!/usr/bin/env bash
# safety!
# makes these commands interactive,
# i.e. prevents accidental overwrites
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'

# this is .. the opposite of safe actually
alias rmrf='rm -rf' # read mail really fast :)
